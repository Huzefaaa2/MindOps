# OTEL Logs

This directory stores logs exported by the OpenTelemetry Collector.

- `logs.json`: File exporter output (created at runtime).

If the file is missing, restart the collector after ensuring this directory exists.
